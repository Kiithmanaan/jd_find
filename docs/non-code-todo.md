# 非代码类 TODO

本文档只列非代码类事项：需要进一步确认、设计、写文档、制定 SOP、准备环境或产出配置口径的工作。具体编码任务另行拆分。

## 1. 产品与验收

- 编写 `docs/demo-script.md`，覆盖本地启动、发起 SearchRun、查询结果、AI 审计、Failed/Cancelled/Interrupted 演示。
- 编写 `docs/acceptance-checklist.md`，列出第一阶段验收项、验证方式、通过标准、当前状态。
- 准备第一阶段 demo 数据集和演示岗位画像。
- 输出用户启动 SearchRun 时目标数量配置的默认 UI 文案和上下限提示。
- 输出候选人汇总页两个 Tab 的文案：
  - 当前版本已评估。
  - 未按当前版本评估。
- 输出匹配分悬浮弹窗的展示顺序和空状态文案。

## 2. 浏览器插件协议

- 输出浏览器插件接入协议文档。
- 写入插件登录流程。
- 写入 Plugin Token 的获取、保存和过期策略。
- 写入插件候选人单条提交协议。
- 写入插件候选人批量提交协议。
- 写入插件附件上传协议。
- 写入插件批次状态上报协议。
- 写入插件轮询 SearchRun 状态的频率建议。
- 写入插件收到以下错误码后的动作：
  - `ValidationError`
  - `SearchRunCompleted`
  - `SearchRunCancelled`
  - `SearchRunFailed`
  - `AuthError`
  - `RateLimited`
  - `InternalError`
- 单独制定浏览器插件风控规避调度策略文档；主工程不接收、不处理风险状态。

## 3. AI 与 Agent

- 等待并接收用户提供的已训练 skill，用于生成岗位画像和评级简历逻辑参考。
- 基于已训练 skill 输出软性条件生成 agent 的输入、输出、版本策略。
- 基于已训练 skill 输出匹配 agent 的输入、输出、版本策略。
- 写入 AI 推荐结论的枚举 + 自然语言说明格式。
- 写入合适点、不合适点、风险点每类最多 3 条的展示格式。
- 输出 prompt version 和 agent version 的命名规则。
- 输出 AI 审计页完整 JSON 的展示方式。
- 输出 AI 调用失败审计的可见字段。
- 制定 AI 输出质量验收样例。

## 4. 硬性条件配置

- 设计硬性条件维度配置表的 seed 内容。
- 制定第一批城市配置项和别名。
- 制定第一批行业配置项和别名。
- 制定学历等级和别名：
  - 大专
  - 本科
  - 硕士
  - 博士
- 输出全文关键词归一化规则中的常见符号清单。
- 输出硬性规则编辑器的交互细节：
  - 淘汰规则区。
  - 合格规则区。
  - 单条规则 `AND/OR` 切换。
  - 条件新增、删除、排序。
- 输出硬筛 trace 的前端展示位置和字段文案。

## 5. 前端产品设计

- 输出 React/Vite 极简运维页的信息架构稿。
- 设计登录页。
- 设计 JobProfile 列表页。
- 设计 JobProfile 创建/编辑/确认页。
- 设计 JobProfile 详情页。
- 设计 SearchRun 详情页。
- 设计候选人列表。
- 设计匹配分悬浮详情。
- 设计 AI 审计详情弹窗或页面。
- 设计附件下载入口。
- 设计 Failed、Cancelled、Interrupted 状态展示。
- 设计插件提交进度和原始提交数量展示。
- 优先设计版本与重新评估相关页面和交互。

## 6. 认证与账号

- 输出第一阶段生产初始用户邮箱和交付方式。
- 输出 Web JWT 过期后的前端提示。
- 输出 Plugin Token 过期后的插件提示。
- 输出密码复杂度最低要求。
- 修改密码功能第一阶段后置，后续再设计。
- 忘记密码功能第一阶段后置，后续再设计。

## 7. 部署与运维

- 明确第一阶段云服务器规格。
- 明确生产域名。
- 输出 Caddy HTTPS 反向代理部署说明。
- 输出生产 `.env` 字段说明。
- 输出 Docker Compose 生产部署步骤。
- 输出 PostgreSQL volume 规划。
- 输出 Redis 持久化策略。
- 输出每日 `pg_dump` 备份方案。
- 写入备份保留 14 天策略，并明确备份文件保存位置。
- 输出恢复演练步骤。
- 输出日志查看和故障排查步骤。

## 8. API 与文档

- 编写 OpenAPI YAML。
- 覆盖现有 API 和后续插件 ingestion API。
- README 保持简洁，只保留项目入口、快速启动和文档索引。
- 新增开发手册。
- 新增部署手册。
- 新增插件接入协议。
- 新增运营与排障手册。
- 新增 AI 审计说明。
- 新增硬性条件配置说明。

## 9. 协作与质量

- 写入 GitHub Actions CI 规则说明。
- 写入 feature branch 命名规范：`codex/中文短描述`。
- 写入中文 + 类型前缀的提交规范。
- 写入合并到 `main` 前本地必须执行的全量检查命令。
- 暂不要求 PR review，但需要保持 `main` 可运行。
- 输出版本发布记录格式。

## 10. 待后续重点复核的设计风险

- 候选人数据合规风险：MVP 版本暂不考虑，正式上线前再复核。
- Web 权限模型风险：MVP 版本暂不考虑，正式多人使用前再复核。
- 插件风控边界：已确认主工程只接收插件提交的数据，不接收/处理风险状态，不承担抓取风控策略。
- 插件增量聚合风险：近期实现队列和缓冲策略，按 30 秒窗口 / 20 条阈值收敛。
- JobProfileVersion、重新评估和候选人汇总视图风险：近期优先做领域模型调整，顺序为先模型后前端。
- AI 契约冲突风险：近期优先移除禁止推荐旧限制，并扩展 `MatchAssessment`。
